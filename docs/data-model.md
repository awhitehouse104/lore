# Lore data model

Lore stores authoritative knowledge as UTF-8 Markdown with YAML frontmatter. Raw sources are append-oriented and hash-verified; synthesized pages are mutable and cite sources.

The complete v0.1 source and page schemas will be documented as implementation milestones land.

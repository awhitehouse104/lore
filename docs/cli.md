# Lore CLI

Lore v0.1 exposes `init`, `capture`, `search`, `read`, `lint`, `recent`, and `version`. All machine-readable responses use JSON schema version 1.

Detailed usage and output contracts will be completed alongside the commands.
